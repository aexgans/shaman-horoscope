<template>
  <div class="app">
    <!-- Шапка -->
    <header class="header">
      <h1>🎂 Анализатор Даты Рождения</h1>
      <p class="subtitle">Введите дату рождения для расчета характеристик</p>
    </header>

    <!-- Основное содержимое -->
    <main class="main-content">
      <!-- Левая колонка - форма -->
      <div class="form-section">
        <div class="card">
          <h2>📅 Введите данные</h2>
          
          <!-- Поле для даты -->
          <div class="input-group">
            <label for="birthdate">Дата рождения:</label>
            <input 
              type="date" 
              id="birthdate"
              v-model="birthDate"
              :max="maxDate"
              class="date-input"
            />
          </div>

          <!-- Кнопка расчета -->
          <button 
            @click="calculate"
            class="calculate-btn"
            :disabled="!birthDate"
          >
            🔮 Рассчитать характеристики
          </button>

          <!-- Быстрый выбор -->
          <div class="quick-dates">
            <p>Быстрый выбор:</p>
            <div class="quick-buttons">
              <button 
                v-for="date in quickDates"
                :key="date.label"
                @click="selectDate(date.date)"
                class="quick-btn"
              >
                {{ date.label }}
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Правая колонка - результаты -->
      <div class="results-section">
        <div class="card">
          <h2>📊 Результаты</h2>
          
          <div v-if="loading" class="loading">
            <div class="spinner"></div>
            <p>Идет расчет...</p>
          </div>

          <div v-else-if="results" class="results">
            <div class="result-item">
              <span class="label">Дата рождения:</span>
              <span class="value">{{ formattedDate }}</span>
            </div>
            <div class="result-item">
              <span class="label">Возраст:</span>
              <span class="value">{{ results.age }} лет</span>
            </div>
            <!-- Здесь будут другие результаты -->
            <div class="placeholder">
              <p>Здесь будут отображаться:<br>
              - Знак зодиака<br>
              - Год по восточному календарю<br>
              - Нумерологическое число<br>
              - И другие параметры</p>
            </div>
          </div>

          <div v-else class="empty-results">
            <p>Введите дату рождения и нажмите "Рассчитать"</p>
          </div>
        </div>

        <!-- Статус данных -->
        <div class="card status-card">
          <h3>ℹ️ Информация</h3>
          <p>Данные актуальны до 2050 года</p>
          <p class="small">Версия приложения: 1.0</p>
        </div>
      </div>
    </main>

    <!-- Подвал -->
    <footer class="footer">
      <p>© 2026 Анализатор Даты Рождения</p>
    </footer>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

// Данные
const birthDate = ref('')
const loading = ref(false)
const results = ref(null)

// Максимальная дата (сегодня)
const today = new Date()
const maxDate = computed(() => today.toISOString().split('T')[0])

// Быстрые даты
const quickDates = [
  { label: 'Сегодня', date: maxDate.value },
  { label: '1 января 2000', date: '2000-01-01' },
  { label: '1 января 1990', date: '1990-01-01' },
  { label: 'Новый год 2024', date: '2024-01-01' }
]

// Форматированная дата
const formattedDate = computed(() => {
  if (!birthDate.value) return ''
  const date = new Date(birthDate.value)
  return date.toLocaleDateString('ru-RU')
})

// Функции
const calculate = () => {
  if (!birthDate.value) return
  
  loading.value = true
  results.value = null
  
  // Имитация расчета
  setTimeout(() => {
    const birth = new Date(birthDate.value)
    const age = today.getFullYear() - birth.getFullYear()
    
    results.value = {
      age: age,
      zodiac: 'Будет рассчитан',
      animalYear: 'Будет рассчитан',
      numerology: 'Будет рассчитан'
    }
    
    loading.value = false
  }, 1000)
}

const selectDate = (date) => {
  birthDate.value = date
}
</script>

<style>
/* Базовые стили */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  min-height: 100vh;
}

.app {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

/* Шапка */
.header {
  text-align: center;
  margin-bottom: 40px;
  padding: 20px;
}

.header h1 {
  font-size: 2.5rem;
  color: #2d3748;
  margin-bottom: 10px;
}

.subtitle {
  font-size: 1.1rem;
  color: #4a5568;
}

/* Основной контент */
.main-content {
  display: grid;
  grid-template-columns: 1fr;
  gap: 30px;
}

@media (min-width: 768px) {
  .main-content {
    grid-template-columns: 1fr 1fr;
  }
}

/* Карточки */
.card {
  background: white;
  border-radius: 12px;
  padding: 25px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  margin-bottom: 20px;
}

.card h2 {
  font-size: 1.5rem;
  color: #2d3748;
  margin-bottom: 20px;
  display: flex;
  align-items: center;
  gap: 10px;
}

/* Форма */
.input-group {
  margin-bottom: 20px;
}

.input-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 500;
  color: #4a5568;
}

.date-input {
  width: 100%;
  padding: 12px;
  border: 2px solid #e2e8f0;
  border-radius: 8px;
  font-size: 1rem;
  transition: border-color 0.2s;
}

.date-input:focus {
  outline: none;
  border-color: #4299e1;
}

/* Кнопки */
.calculate-btn {
  width: 100%;
  padding: 15px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: transform 0.2s;
  margin-bottom: 20px;
}

.calculate-btn:hover:not(:disabled) {
  transform: translateY(-2px);
}

.calculate-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* Быстрый выбор */
.quick-dates {
  margin-top: 20px;
}

.quick-dates p {
  margin-bottom: 10px;
  font-weight: 500;
  color: #4a5568;
}

.quick-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.quick-btn {
  padding: 8px 16px;
  background: #edf2f7;
  border: 1px solid #e2e8f0;
  border-radius: 6px;
  font-size: 0.9rem;
  cursor: pointer;
  transition: background 0.2s;
}

.quick-btn:hover {
  background: #e2e8f0;
}

/* Результаты */
.loading {
  text-align: center;
  padding: 40px 0;
}

.spinner {
  width: 40px;
  height: 40px;
  border: 3px solid #e2e8f0;
  border-top-color: #667eea;
  border-radius: 50%;
  margin: 0 auto 15px;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.result-item {
  display: flex;
  justify-content: space-between;
  padding: 12px 0;
  border-bottom: 1px solid #e2e8f0;
}

.result-item:last-child {
  border-bottom: none;
}

.label {
  color: #4a5568;
  font-weight: 500;
}

.value {
  color: #2d3748;
  font-weight: 600;
}

.placeholder {
  margin-top: 20px;
  padding: 20px;
  background: #f7fafc;
  border-radius: 8px;
  text-align: center;
  color: #718096;
}

.empty-results {
  text-align: center;
  padding: 40px 0;
  color: #a0aec0;
}

/* Статус */
.status-card {
  background: #ebf8ff;
  border: 1px solid #bee3f8;
}

.status-card h3 {
  color: #2b6cb0;
  margin-bottom: 10px;
}

.small {
  font-size: 0.9rem;
  color: #718096;
  margin-top: 10px;
}

/* Подвал */
.footer {
  text-align: center;
  margin-top: 40px;
  padding-top: 20px;
  border-top: 1px solid #e2e8f0;
  color: #718096;
  font-size: 0.9rem;
}
</style>
